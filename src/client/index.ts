/**
 * dsh-model-toggle — browser half.
 *
 * Phase 1 skeleton: proves the client bundle loads through the module
 * loader. Later phases register the settings card into the
 * `settings.plugin.item` slot.
 */
export const name = 'dsh-model-toggle'

export function apply(): void {
  console.info('dsh-model-toggle: client half loaded')
}
